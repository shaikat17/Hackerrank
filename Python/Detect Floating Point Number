# https://www.hackerrank.com/challenges/introduction-to-regex/problem

import re
for _ in range(int(input())):
    print(bool(re.match("^[+,-]?\d*\.\d+$",input()))) # Starts with sign or number (Zero or more occurance) then point then ends with number (minimum one occurance)
    
